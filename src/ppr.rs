//! Add a pretty printer for the [`crate::nfe::Nfe`] type. It does print
//! in a legible way the zure's constructs.

#[macro_export]
macro_rules! show_nfe_of {
    ($e:expr) => {
        todo!("show_nfe_of!({:?})", $e)
    };
}